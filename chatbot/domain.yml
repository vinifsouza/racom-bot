version: "2.0"

intents:
  - nlu_fallback
  - intent_welcome
  - intent_about_me
  - intent_help
  - intent_finish

responses:
  utter_adeus:
  - text: "Tchau, tchau! Até a próxima."

actions:
  - action_welcome
  - action_about_me
  - action_default_fallback
  - action_help

session_config:
  session_expiration_time: 10
  carry_over_slots_to_new_session: true
